return {
    git = {
        enable = true,
    },
    view = {
        side = "right",
        width = 30,
    },
    renderer = {
        highlight_opened_files = "all",
        indent_markers = {
            enable = true,
        },
        highlight_git = true,
        icons = {
            show = {
                git = true,
            },
        },
    },
}
